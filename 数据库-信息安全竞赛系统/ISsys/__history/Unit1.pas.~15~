unit Unit1;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, ExtCtrls, jpeg;

type
  Tlogin = class(TForm)
    usr: TEdit;
    psd: TEdit;
    确定: TButton;
    取消: TButton;
    back: TImage;
    用户名: TLabel;
    密码: TLabel;
    procedure 取消Click(Sender: TObject);
    procedure 确定Click(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  login: Tlogin;

implementation

{$R *.dfm}



procedure Tlogin.确定Click(Sender: TObject);
begin
      //label3.Caption := '学号位：' + edit1.Text + '姓名为：' + edit2.Text;
end;

procedure Tlogin.取消Click(Sender: TObject);
begin
    usr.Clear;
    psd.Clear;
    usr.SetFocus;
end;

end.
